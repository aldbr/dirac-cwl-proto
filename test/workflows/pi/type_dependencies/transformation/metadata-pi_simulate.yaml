# Override hints from CWL workflow
$namespaces:
  dirac: "test/workflows/test_meta/schemas/dirac-metadata.json"
dirac:execution-hooks:
  configuration:
    num_points: 2000  # Override default 1000 from CWL
