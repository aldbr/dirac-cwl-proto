$namespaces:
  dirac: "test/workflows/test_meta/schemas/dirac-metadata.json"
simulate:
  dirac:execution-hooks:
    hook_plugin: PiSimulatePlugin
gathering:
  dirac:execution-hooks:
    hook_plugin: PiGatherPlugin
    group_size:
      input-data: 1
    configuration: {}
