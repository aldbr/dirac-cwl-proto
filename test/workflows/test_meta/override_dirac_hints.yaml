$namespaces:
  dirac: "./schemas/dirac-metadata.json#"
cwltool:overrides:
  test_meta.cwl#:
    hints:
      dirac:execution-hooks:
        hook_plugin: "QueryBasedPlugin"
        configuration:
          site: "Paranal"

      dirac:scheduling:
        platform: x86_64
        priority: 5
        sites: ["CSCS", "PIC"]
