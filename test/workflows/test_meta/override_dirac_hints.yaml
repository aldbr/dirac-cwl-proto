cwltool:overrides:
  test_meta.cwl:
    hints:
      - class: dirac:ExecutionHooksHint
        hook_plugin: "QueryBasedPlugin"
        configuration:
          site: "Paranal"

      - class: dirac:SchedulingHint
        platform: x86_64
        priority: 5
        sites: [ "CSCS", "PIC" ]
