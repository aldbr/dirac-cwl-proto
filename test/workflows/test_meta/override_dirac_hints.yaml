$namespaces:
  dirac: "./schemas/dirac-metadata.json#"
cwltool:overrides:
  test_meta.cwl#:
    hints:
      dirac:metadata:
        type: "TaskWithMetadataQuery"
        query_params:
          site: "Paranal"

      dirac:description:
        platform: x86_64
        priority: 5
        sites: ["CSCS", "PIC"]
