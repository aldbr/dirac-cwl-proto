$namespaces:
  dirac: "./schemas/dirac-metadata.yaml#"
cwltool:overrides:
  test_meta.cwl#:
    hints:
      dirac:metadata:
        type: 'TaskWithMetadataQuery'
        query_params:
          site: 'Paranal'

      dirac:description:
        platform: x86_64
        priority: 5
        sites: ['CSCS', 'PIC']

cwltool:overrides:
  test_meta.cwl#:
    hints:
      dirac:metadata:
        type: 'TaskWithMetadataQuery'
        query_params:
          site: 'LaPalma'

      dirac:description:
        platform: arm64
        priority: 3
        sites: ['CSCS', 'PIC']