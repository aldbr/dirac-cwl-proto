$namespaces:
  dirac: "./schemas/dirac-metadata.json#"
cwltool:overrides:
  test_meta.cwl#:
    hints:
      dirac:data-management:
        metadata_class: 'TaskWithMetadataQueryPlugin'
        query_params:
          site: 'Paranal'

      dirac:job-execution:
        platform: x86_64
        priority: 5
        sites: ['CSCS', 'PIC']

cwltool:overrides:
  test_meta.cwl#:
    hints:
      dirac:data-management:
        metadata_class: 'TaskWithMetadataQueryPlugin'
        query_params:
          site: 'LaPalma'

      dirac:job-execution:
        platform: arm64
        priority: 3
        sites: ['CSCS', 'PIC']